#include <iostream>

using namespace std;

void print(const char *s, int start, int end) {
    for (int i = start; i != end; ++i) {
        cout << s[i];
    }
    cout << endl;
}

int main (int argc, char *argv[]) {
    char s[] = "Hello, World!";

    // print(s, 0, 5);

    int start = -1;
    int end = 0;
    if (start = !end) {
        cout << "start = !end" << endl;
    } else {
        cout << "not true" << endl;
    }
    
    return 0;
}
